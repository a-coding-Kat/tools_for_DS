# Imagine you have a list of numbers, and you want to find every number higher than 10.

num_list = [1, 5, 67, 34, 28, 3, 6, 90]

larger_than_10 = []
for num in num_list:
  if num > 10:
    larger_than_10.append(num)

print(larger_than_10)
    
